"""
Daily Aggregation Scheduler Worker
----------------------------------

Назначение:
Этот воркер отвечает за автоматический запуск суточной агрегации метрик
(site_daily_aggregate) в тот момент, когда на сайте заканчиваются сутки
по локальной таймзоне сайта.

Воркеры исполнения НЕ выполняет саму агрегацию.
Он только:
  • определяет, для каких сайтов наступил "конец дня"
  • вызывает функцию агрегатора для этих сайтов
  • следит за тем, чтобы каждый день считался ровно один раз

Почему отдельный воркер:
  • агрегатор может быть тяжёлым (много сессий, статистика)
  • запуск по расписанию должен быть управляемым и изолированным
  • в будущем сюда добавится ретраев, логирование, метрики и очередь задач


------------------------------------------------------------
Основная логика работы
------------------------------------------------------------

1) Воркера запускается постоянно (фоновый процесс внутри контейнера).

2) Каждую ПЕРВУЮ минуту каждого часа он выполняет шаг:

   • читает таблицу sites
   • для каждого сайта берёт timezone
   • вычисляет локальное время сайта (по ZoneInfo)

3) Проверяет условие «только что наступили новые сутки»:
   • локальное время сайта = HH:MM == 00:01
   • значит, предыдущий день завершился полностью
   • можно безопасно агрегировать суточные данные

4) Для таких сайтов воркер:
   • определяет отчётную дату = вчера (по таймзоне сайта)
   • проверяет таблицу site_daily_metrics:
        - нет ли уже записи за эту дату (защита от дублей)
        - reported неважно — ключ по (site_url, date_local)
   • если записи НЕТ → запускает агрегатор

5) Агрегатор вызывается как функция / subprocess:

   Варианты реализации (решим позже):
     • import summary.metrics.site_daily_aggregate.main(...)
     • или запуск через `python …site_daily_aggregate.py <site_url>`

6) После вызова:
   • запись появляется в site_daily_metrics
   • воркер помечает событие в логах

7) Если агрегатор вернул ошибку:
   • писать в лог
   • запланировать ретрай через 1 час (TODO в будущем)


------------------------------------------------------------
Будущие доработки (закладки на развитие)
------------------------------------------------------------

• Добавить очередь задач (не запускать много сайтов одновременно)
• Ограничение на частоту повторных агрегаций
• Метрики/логирование в отдельную таблицу
• Ретраи при падениях сети/БД
• Блокировка "aggregation in progress"
• Возможность ручного триггера из админки
• Поддержка недельной/месячной агрегации
• Выборочный запуск только по активным сайтам
• Фильтр по тарифам (в бесплатных — реже)

------------------------------------------------------------
Минимальный будущий алгоритм (псевдокод)
------------------------------------------------------------

loop every 1 minute:
    now_utc = datetime.utcnow()

    sites = db.fetch("select site_url, timezone from sites where is_active=true")

    for site in sites:
        local_time = now_utc.astimezone(ZoneInfo(site.timezone))

        if local_time.hour == 0 and local_time.minute == 1:
            report_date = (local_time.date() - 1 day)

            if not exists(site_daily_metrics[site, report_date]):
                run_daily_aggregator(site_url)

sleep(60 seconds)


------------------------------------------------------------
ВАЖНО:
Файл — это только архитектурная заготовка.
Реальную реализацию добавим после проверки стабильности пайплайна
и утверждения бизнес-логики.
------------------------------------------------------------
"""
